{
	"title": "should throw InsightError if COLUMNS contains a string that isn't an apply key or regular key",
	"input": {
		"WHERE": {},
		"OPTIONS": {
			"COLUMNS": [
				"AAAAAAAAA",
				"courses_title",
				"overallAvg"
			]
		},
		"TRANSFORMATIONS": {
			"GROUP": [
				"courses_title"
			],
			"APPLY": [
				{
					"overallAvg": {
						"AVG": "courses_avg"
					}
				}
			]
		}
	},
	"errorExpected": true,
	"with": "InsightError"
}
