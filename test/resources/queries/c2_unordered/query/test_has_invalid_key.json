{
	"title": "should throw InsightError if query has WHERE, OPTIONS, and an invalid key",
	"input": {
		"WHERE": {},
		"OPTIONS": {
			"COLUMNS": [
				"courses_title",
				"overallAvg"
			]
		},
		"TRANSFORM": {
			"GROUP": [
				"courses_title"
			],
			"APPLY": [
				{
					"overallAvg": {
						"AVG": "courses_avg"
					}
				}
			]
		}
	},
	"errorExpected": true,
	"with": "InsightError"
}
