{
	"title": "should throw InsightError if query has OPTIONS and TRANSFORMATIONS but no WHERE",
	"input": {
		"OPTIONS": {
			"COLUMNS": [
				"courses_title",
				"overallAvg"
			]
		},
		"TRANSFORMATIONS": {
			"GROUP": [
				"courses_title"
			],
			"APPLY": [
				{
					"overallAvg": {
						"AVG": "courses_avg"
					}
				}
			]
		}
	},
	"errorExpected": true,
	"with": "InsightError"
}
