{
	"title": "should throw InsightError if query has WHERE and TRANSFORMATIONS but no OPTIONS",
	"input": {
		"WHERE": {},
		"TRANSFORMATIONS": {
			"GROUP": [
				"courses_title"
			],
			"APPLY": [
				{
					"overallAvg": {
						"AVG": "courses_avg"
					}
				}
			]
		}
	},
	"errorExpected": true,
	"with": "InsightError"
}
